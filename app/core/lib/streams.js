const meta = attract('core/streams/meta');
const resize = attract('core/streams/resize');
const filter = attract('core/streams/filter');
const response = attract('core/streams/response');

module.exports = {
  meta,
  resize,
  filter,
  response
};
